
		链式前向星 
		//比邻接表还省空间
  运用静态数组来模拟邻接表	     struct{
				int to;//边指向的顶点
				int next;//同个顶点下一条边的序号
				int w;//边权值								}edge[10005];
                                                                   head[u];
//代表以顶点u为起点的第一条边
//增加边时head倒着存储，就是存储最后添加的以u为起点的边，这样才能实现next的添加
//初始化 next与to都为-1